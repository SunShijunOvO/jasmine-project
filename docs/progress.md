# 项目进度

最近更新：2026-09-17；日期采用 Asia/Shanghai。里程碑和截止约束见 [项目计划](project-plan.md)。本文只维护项目事实、任务和工程问题；知识笔记、掌握情况与每日学习回顾在本地独立管理。

## 当前快照

| 项目 | 当前事实 |
|---|---|
| 当前阶段 | M1 的 MVP 范围已确认，正在 M2 最小设计与持久化基础实现；尚未完成可持久保存的 F1 |
| 已完成的准备 | 项目记忆与暂定路线图已建立；项目文档与个人学习记录已分离；学习者确认非竞赛期平均每天可投入约 6 小时 |
| 已完成的应用功能 | `GET /health` 已通过本机 HTTP 验证；临时 `POST /applications` 已验证请求成功及 422 校验失败路径，但仍只回显数据，不持久保存 |
| 当前任务 | PostgreSQL、应用连接配置和 `Application` ORM 模型已准备完成；创建并检查首张 `applications` 表 |
| 最近验证 | 项目角色通过 TCP 密码认证连接 `internship_tracker`，且非超级用户；SQLAlchemy Engine 已由学习者实际建立并关闭连接且无异常。模型已明确使用非原生 Enum、命名 CHECK 约束；尚未执行建表 |
| Git 状态 | 最近检查显示 `.gitignore`、`README.md`、两份 `docs/` 文档有未提交修改，`backend/` 尚未跟踪；`.env`、本地数据、缓存、学习记录和编辑器配置已确认被忽略；本次未执行 commit 或 push |
| 当前阻塞 | 无已知阻塞；学习者在建表前结束当天学习，数据库中是否存在 `applications` 表尚未验证 |
| 下一小步 | 从项目根目录加载模型，使其登记到 `Base.metadata`，再由学习者调用 `create_all` 创建缺失表 |
| 下一小步完成证据 | 以项目角色执行 `psql` 的 `\d applications`，确认五列、主键及名为 `application_status` 的 CHECK 约束 |

## 已知问题与技术债

技术债指为推进项目有意识保留、将来需要处理的实现取舍。未开始学习或尚未决定的设计，不自动算技术债。

| 类型 | 事项及影响 | 下一次处理时机 |
|---|---|---|
| 待验证 | 当前问题、需求和使用场景来自假设，尚无真实经历或试用证据 | 首位用户开始真实求职记录或进行针对性验证时更新依据 |
| 待设计 | 具体输入规则与接口字段格式待明确；核心端到端验收已有草案，待实现后验证 | M2，围绕最小流程设计 |
| 缺陷 | 尚无应用可供验证，没有已确认应用缺陷 | 实现或试用发现证据时再记录 |
| 技术债 | 暂无已确认技术债 | 发生有意识的实现取舍时，记录影响、原因及处理时机 |

## 每日项目计划与工程交接

开始时依据最近实际结果、可投入时间和里程碑，确定一个项目核心目标及完成证据，最多增加一个可选任务。当天跨会话接续原目标，不重新排整天任务；留出调试和思考时间。

结束或交接时记录实际产出、未完成原因、验证结果、阻塞及下次起点。检查是否值得 commit、该提交表达什么变化、是否已执行，以及项目文档或里程碑是否需要调整。学习知识点、理解证据和每日学习回顾另行记录。

只更新有变化的事实，不抄写完整聊天。当前快照是当前任务的唯一维护位置；下面的历史条目保留阶段成果，不另建重复任务清单。

## 项目阶段记录

### 2026-09-17 · F3 形成与确认

- 当日目标：形成集中查看需求；今日可投入时间尚未提供，先完成这一小步。
- 产出：学习者要求由导师完成 F3，导师据已有场景补写行为、最小可见信息、读取验收与优先级理由，并明确标注内容待确认。
- 检查范围：文档覆盖等待回复、有明确下一步安排及无记录情况；没有应用运行或测试证据。
- 确认：学习者阅读后明确表示符合预期，F3 更新为已确认需求；此确认不作为独立推导能力的证据。
- 使用范围：学习者确认第一版仅本人在开发电脑上使用，明确希望省去的是其他设备通过网络访问，而非禁止程序内部通信；已同步项目计划及路线图的部署边界。
- F2 修订：学习者提出主动纠错并沿用持久保存要求，随后确认将 F2 扩展为进展更新与主动纠错。导师按授权修改文档，明确未选择修改的信息和其他岗位记录不受影响；原 09-16 记录保留为历史，现行需求以修订后的 F2 为准。
- MVP 确认：学习者同意 F1—F4 四项功能、停止追踪保留记录及延后范围。导师同步需求与核心端到端验收草案；尚无实现结果。
- 节奏：依学习者要求加快设计，提前开始最小数据设计，不等待原暂定 M2 日期；竞赛停工窗口不变。
- 模型确认：学习者同意只保存当前进度与最近一个下一步安排，暂不保留完整历史；具体输入规则仍待细化。
- 架构确认：用户同意单主页面、本机前端/后端/数据库分工及新增后重新读取的首条流程；导师补充两个最小接口草案。只读定位工具，不安装依赖、不启动应用。
- 环境更正：学习者指出已使用 Conda；只读检查 .vscode/settings.json 确认 Conda 管理配置，但该文件未给出具体环境名与解释器路径。撤回创建 .venv 的步骤，沿用已有环境。
- 环境核实：用户提供 study 的绝对路径；通过该环境解释器确认版本与依赖元数据，当前尚未安装首个后端所需的 FastAPI 与 Uvicorn。
- 安装核对：学习者报告安装完成；导师只读包元数据确认 FastAPI 0.138.0 与 Uvicorn 0.52.4 位于 study 环境。backend 目录尚未创建。
- 交接：下一步由学习者编写最小应用入口与 GET /health；该接口仅检查应用能否响应，不表示数据库或业务功能已可用。此次文档阶段适合由学习者保存“确定 MVP 范围与访问边界”的提交；本次未执行 commit 或 push。
- 首条运行证据：学习者亲手启动 Uvicorn。首次访问因请求 `/` 而得到 404，依据终端日志核对路径后访问 `/health`，浏览器得到 `{"status":"ok"}`，终端记录 200；应用对象、路由、处理函数和 HTTP 服务链路已完成最小验证。
- 请求校验证据：学习者通过 Swagger UI 提交合法新增请求并得到 200；纯空格公司名经统一去空白后得到 422，错误定位到 `body.company_name`。当前 POST 仍只回显请求，不代表 F1 已保存。
- 持久化基础：Docker Hub 不可达后曾选择 SQLite 过渡，随后由 D14 改为本机 PostgreSQL。学习者安装并验证 PostgreSQL 18.6 与 psycopg 3.3.5，创建最小权限登录角色和由其拥有的 `internship_tracker` 数据库；TCP 密码连接、非超级用户属性及 SQLAlchemy Engine 连接均已实际验证。
- 配置与模型：本地 `.env` 已确认受 Git 忽略；应用用 `pydantic-settings` 读取分项连接配置，用 `URL.create` 组装 `postgresql+psycopg` 地址。`Application` ORM 模型包含五列，并为状态列配置非原生 Enum 与命名 CHECK；表尚未创建。
- 当日交接：学习者在建表前结束。下次直接导入模型、调用 metadata 建表，并用 `\d applications` 检查实际结构；不重复 PostgreSQL 安装、角色、数据库或连接配置。当前变化已形成值得保存的“建立后端校验与 PostgreSQL 持久化基础”阶段，但本次未 commit 或 push。

### 2026-09-15 · 文档准备与职责分离

- 目标与产出：先完成五份初始文档；随后按用户要求迁出学习记录，同步项目文档入口，项目范围与路线图未改变。
- 实际状态：尚未开始产品需求分析，没有应用代码或未完成的编码任务。
- 验证：初次文档的存在性、14 处内部链接、日期和内容一致性检查通过；本次调整后的文件链接、入口、内容分工与授权边界审查通过，用户已有的 .gitignore 内容保持原样。
- Git：未执行 commit；本次阶段适合保存“建立项目文档并分离个人学习记录”的变更，由学习者理解并检查后亲手提交。
- 交接：下次从一个具体求职信息管理场景开始；跨日时先重新确认当日时间与核心目标。

### 2026-09-15 · 假设需求分析与首个功能需求

- 目标与产出：从求职信息分散的假设场景出发，形成问题分析、核心用户需求和“集中检查所有岗位”的核心使用场景；完成 F1“新增并持久保存一个已投递岗位”。
- 范围结论：岗位记录纳入已确认的下一步日期和地点；自动添加日历、提醒、冲突检测及面试复盘暂不进入当前核心范围。完整 MVP 尚未定界。
- F1：最小信息为投递时间、公司名称、岗位名称和当前进度；刷新或退出重进后仍能看到一致内容，作为持久保存的外部验收证据。
- 验证：上述项目计划内容由学习者亲手填写，导师逐项只读检查通过；需求依据仍是假设，不代表已经真实验证，也没有应用运行结果。
- 时间：学习者确认当天可投入 3 小时；非竞赛期平均每天可投入时间最初填写为约 8 小时，随后调整为约 6 小时；实际用时未统计。
- 文档授权：学习者授权导师今后在每天结束任务时自动同步 `docs/` 下现有及未来新增的项目文档；不扩展到源码、Git、运行测试或部署。
- Git：未执行 commit 或 push；当前项目文档阶段已有值得保存的变化，提交仍由学习者理解并亲手执行。
- 交接：下次直接从 F2 的进展更新行为与验收结果开始，不重复问题分析、用户需求、核心使用场景或 F1。

### 2026-09-16 · 进展更新需求与查看范围判断

- 目标与产出：完成 F2“收到新进展后更新已有岗位”的推导，并由学习者亲手写入项目计划。
- F2：允许更新特定岗位的进度，补充下一步时间、地点和线上或线下方式，同时保留公司、岗位和原投递时间；刷新或退出重进后仍应显示正确的新旧信息。
- 优先级：若不能更新，系统只能保存初始投递，后续流程信息仍会分散，无法持续支持集中检查。
- 验证：导师只读检查确认 F2 的行为、信息变化、持久保存验收和优先级理由齐全，未混入界面或技术实现。
- 范围判断：按进度筛选和按下一步时间排序是可能提升查看效率的候选能力，但经两条记录反例确认，它们不是核心集中检查场景成立的必要条件；是否进入 MVP 仍待后续判断。
- 时间与交接：学习者因当天忙其他事情结束学习，未提供实际用时；F3 尚未形成，下一次从集中查看的用户行为、最小可见信息、验收和优先级理由继续。
- Git：本次未执行 commit 或 push；当前文档变化已值得保存，是否提交及具体操作仍由学习者后续亲手完成。
